{
  "name": "base-project-config",
  "version": "1.0.0",
  "description": "A project with basic configuration for reusing in another ones",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepush": "git add . ",
    "push": "git commit -m $1",
    "pospush": "git push"
  },
  "bin": {
    "run-test": "./run-test.sh",
    "compile": "./compile.sh"
  },
  "author": "Farenheith",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^13.1.1",
    "gts": "^1.1.2",
    "sinon-chai-calls-assertion": "^1.3.0",
    "typescript": "^3.7.4"
  }
}
